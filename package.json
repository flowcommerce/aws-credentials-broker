{
  "name": "aws-credentials-broker",
  "version": "0.0.1",
  "description": "AWS Credentials Broker - Grants temporary AWS credentials for Google federated users",
  "author": "Flow",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/flowcommerce/aws-credentials-broker.git"
  },
  "scripts": {
    "build": "node_modules/.bin/parcel build public/index.html --out-dir templates --out-file index.tmpl --public-url ./dist/"
  },
  "dependencies": {
    "lodash": "^4.17.21",
    "react": "^16.4.2",
    "react-dom": "^16.4.2"
  },
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0",
    "babel-preset-react": "^6.24.1",
    "parcel-bundler": "^1.11.0"
  }
}

